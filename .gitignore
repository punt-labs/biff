# === Punt Labs canonical .gitignore ===
# Source of truth: .bin/gitignore-canonical.template
# Rollout:         .bin/gitignore-canonical-rollout.sh
# Repo-specific additions belong BELOW the "# === Repo-specific ===" marker.
# Everything above it is managed by the rollout script.

# --- Secrets & local overrides ---
.env
.env.*
.env*.local
!.env.example
.envrc.local
.biff.local
.claude/settings.local.json
.claude/settings.local.*
.claude/mcp.local.json
.mcp.local.json
*.local
*.local.*
.*.local
.*.local.*

# --- Punt Labs runtime state (never commit) ---
.punt-labs/local/
.punt-labs/**/local/**
.punt-labs/**/*.local.*
.punt-labs/quarry/captures/
.punt-labs/vox/ephemeral/
.punt-labs/vox/vox.local.md
.punt-labs/lux.md

# --- Claude Code runtime state ---
.claude/*
!.claude/settings.json
!.claude/commands/
!.claude/hooks/
!.claude/agents/
!.claude/rules/
!.claude/skills/
.claude/scheduled_tasks*
.claude/worktrees/
.claude/projects/
.claude/beadle.poll.*
*/**/.claude/

# --- Lock files (except uv.lock) ---
.*.lock
*.punt-import.lock
.CLAUDE.md.lock
.CLAUDE.md.punt-import.lock
**/.create.lock

# --- Beads runtime state ---
.beads/bd.sock*
.beads/beads.db*
.beads/daemon-*
.beads/daemon.log
.beads/daemon-error
.beads/.local_version

# --- Scratch / temp ---
# .tmp/ is the repo-local TMPDIR (set via .envrc). We keep the directory but
# ignore its contents. Do NOT add a bare `*.tmp` pattern here — it would match
# the `.tmp` directory itself, preventing git from descending, and then the
# `!.tmp/.gitkeep` negation could not re-include the tracked marker.
.tmp/*
!.tmp/.gitkeep
CLAUDE.md.tmp

# --- Worktrees ---
.worktrees/

# --- OS junk ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db

# --- Editors / IDEs ---
.idea/
.vscode/
.cursor/
.codex/
.accio/
.claude-flow/
*.swp
*.swo
*~

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
dist/
build/
*.egg
*.egg-info/
.eggs/
.mypy_cache/
.pyright/
.pytest_cache/
.pytest-work/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
uv.toml

# --- Node / web ---
node_modules/
.astro/
.vercel/

# --- Go ---
coverage.out
*.test

# --- Swift / Xcode ---
# (Do NOT ignore *.xcodeproj / *.xcworkspace — those directories are tracked;
# only ignore user-specific state INSIDE them.)
xcuserdata/
DerivedData/
CoverageDerived/
*.xccheckout
*.xcresult
*.xcscmblueprint
*.hmap
*.dSYM
*.dSYM.zip
*.ipa
.swiftpm/
Package.pin
Package.resolved
Carthage/Build/
Carthage/Checkout
Pods/
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output
iOSInjectionProject/
playground.xcworkspace
timeline.xctimeline

# --- LaTeX build artifacts ---
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.run.xml
*.synctex.gz
*.toc

# === Repo-specific ===
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/

# Data
lancedb/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Coverage
htmlcov/
.coverage
.coverage.*

# mypy
.mypy_cache/

# pytest
.pytest_cache/
tests/transcripts/

# ruff
.ruff_cache/

# Environment

# LaTeX (root-level artifacts from prfaq.tex compilation)
*.aux
*.log
*.out
*.toc

# MCP (project-local, not part of plugin distribution)
.mcp.json

# The sibling lock a punt CLI takes before mutating a shared host file
# (tool-enable-disable.md 2.4). Per-machine runtime state, and it persists by
# design — unlinking a lock file races the next writer. The pattern is broad
# because the lock is named for its host file and tools differ on the suffix:
# both .CLAUDE.md.lock and .CLAUDE.md.punt-import.lock occur. Mirrors the same
# rule in punt-labs/team; a submodule's .gitignore does not govern this repo.
.*.lock

# Deprecated tooling
.claude-flow/

# OS
.DS_Store

# Claude Code (settings.json is tracked; everything else is local/runtime).
# Use `.claude/*`, not `.claude/`: git cannot re-include a file whose parent
# directory is excluded, so the `!` line below is dead against a bare `.claude/`.
# But a mid-pattern slash anchors to this directory, so `.claude/*` covers only
# the root one where bare `.claude/` covered every depth; `*/**/.claude/`
# restores that. Nested .claude dirs hold settings.local.json, which carries
# machine-local MCP config and absolute-path allow rules — never commit it.
*/**/.claude/
docs/fuzz.sty
docs/*.mf
docs/*.pk
docs/*.tfm
docs/*.aux
docs/*.log
docs/*.fuzz
docs/*.toc
docs/*.out
docs/*.pdf
docs/*.synctex.gz

# Scratch/temp directory (TMPDIR set via .envrc)

# Plugin runtime state (tts, vox, quarry, lux)
.tts/

# Ethos org registry — this repo has NO submodule at .punt-labs/ethos (see
# CLAUDE.md, "No Git Submodules"). A local clone of punt-labs/team at this
# path stays untracked; the only committed file is the one agent identity
# biff's own runtime reads (src/biff/config.py, DES-040 and DES-053).
# The `dir/*` + negation form is required: git cannot re-include a file whose
# parent directory is itself excluded, so each level is opened explicitly.
# NOT covered by the canonical .gitignore template -- restored here after
# the canonical rollout (2026-08-22) stripped it as if it were, which would
# have exposed missions/ and sessions/ (local audit logs, 0700 dirs) to git.
.punt-labs/ethos/*
!.punt-labs/ethos/identities/
.punt-labs/ethos/identities/*
!.punt-labs/ethos/identities/claude.yaml

# Dev scratch files
*.tmp
hook-feedback.md
conv.json
docs/plan-audio-notifications.md
mcp-message/
scripts/demo.md
scripts/quarry-statusline.sh
scripts/test-talk-e2e.sh
Lux.ini

# Local uv overrides (depot find-links, etc.)
uv.toml
